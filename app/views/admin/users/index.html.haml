%h1 Listing Users
%a{:href => '/users/invitation/new'} Invite a User
%h2 Pending Invites
%table.tables
  %thead
    %tr
      %th Name
      %th Email
      %th{:colspan => '3'} Actions
  %tbody
    - @invited_user.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= link_to 'Show', [:admin, user]
        %td= link_to 'Edit', edit_admin_user_path(user)
        - if current_user.id != user.id
          %td= link_to 'Delete', [:admin, user], method: :delete, data: { confirm: 'Are you sure?' }
%h2 Active Users
%table.tables
  %thead
    %tr
      %th Name
      %th Email
      %th{:colspan => '3'} Actions
  %tbody
    - @active_users.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= link_to 'Show', [:admin, user]
        %td= link_to 'Edit', edit_admin_user_path(user)
        - if current_user.id != user.id
          %td= link_to 'Delete', [:admin, user], method: :delete, data: { confirm: 'Are you sure?' }
